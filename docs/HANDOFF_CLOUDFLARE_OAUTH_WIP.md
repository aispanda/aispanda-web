# Cloudflare AI Gateway OAuth — superseded

This handoff is obsolete and must not be used for diagnosis or next steps.

**Superseded by:** the **2026-08-17 — Cloudflare authorization succeeded but browser generation failed** entry in [`docs/ISSUE_LOG.md`](ISSUE_LOG.md).

That entry records the accepted scopes `aig.run` and `ai.read`, relay-only transport, Authenticated Gateway off, and remaining production closure work. Do not follow the earlier `invalid_scope` / `account.ai_gateway_run` investigation; it is historical and wrong.
